class Solution {
    public String getEncryptedString(String s, int k) {
        int len = s.length();
        StringBuilder ans = new StringBuilder();
        for(int i=0;i<len;i++){
            ans.append(s.charAt((i+k)%len));
        }
        return ans.toString(); 
    }
}
